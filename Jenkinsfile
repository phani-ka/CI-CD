pipeline {
    agent any

    stages {
        stage ('Compile Stage') {

            steps {
               
                    sh 'ls -al '
               
            }
        }

        

        stage ('Deployment Stage') {
            steps {
                 sh 'terraform --version'
            }
        }
    }
}
